默认写入 `chatgpt` 查询捷径并展开为官方网页 `https://chatgpt.com`；完整查询等于捷径键时也会正确展开，已有用户升级后自动补齐且删除后不再恢复。
支持在输入框中将 `C:\`、`C:/` 和 `C:` 统一识别为有效盘符路径并按 Windows 路径规范进行搜索。
为 Local Prompt Search 新增复制后恢复原输入窗口焦点能力，减少 `pt` 模板插入时的手动回切操作。
为 Local Prompt Search 增加复制后自动粘贴开关，默认仅复制并回焦，开启后回焦到原窗口并发送 `Ctrl+V`。
为 Browser Bookmarks 增加 Chrome 已打开标签页搜索与跳转能力，输入关键词时可在下拉结果中定位并切换到目标标签页。
在非技术用户使用说明中补充 Chrome 已打开标签页搜索与跳转功能的用途与操作步骤，便于直接上手。
在非技术用户使用说明中补充“为什么需要先输入 b”的简洁说明，减少首次使用时的理解成本。
新增独立的 Browser Tabs 插件并提供 `tab` 关键词入口，实现 Chrome 已打开标签页的独立搜索与跳转。
将 Chrome 标签页搜索入口正式切换为独立 `tab` 插件，并同步移除 Browser Bookmarks 中的重复标签页结果与更新文档说明。
为 `tab` 插件增加基于 Chrome 会话 URL 的辅助匹配能力，支持按网址关键词搜索已打开标签页。
在非技术用户使用说明中补充 `tab` 插件支持按标签页网址关键词搜索的说明与示例，便于直接使用。
新增《随手记功能设计方案》，明确推荐以 `n` 关键词加专属快捷键预填的方式实现轻量笔记模式。
补充《随手记功能设计方案》的逐阶段实现内容，明确从 MVP、检索、管理到结构化扩展的推进路径。
新增 Quick Note 插件第一轮骨架，完成 `n` 关键词接入以及本地 `notes.json` 初始化与读取能力。
Quick Note 第二轮接入最小保存链路，支持 `n 内容` 生成保存结果并将笔记写入本地 `notes.json`。
新增 Quick Note 第二轮保存链路，支持 `n 内容` 直接生成保存结果并写入本地 `notes.json`。
新增 Quick Note 第三轮最近笔记入口，支持按置顶与更新时间展示首页结果，并提供复制与回填查询动作。
新增 Quick Note 第四轮基础管理能力，支持笔记置顶、删除，以及通过查询框回填并更新已有笔记。
新增 Quick Note 第五轮快捷浏览能力，支持 `pinned`、`today`、`recent` 视图与输入时联想历史笔记。
新增 Quick Note 第六轮产品打磨，补齐中英文资源文案并完善首页、搜索与空状态提示。
新增 Quick Note 第七轮搜索排序优化，支持精确命中、前缀命中、完整词命中、置顶与最近更新的综合评分。
新增 Quick Note 第八轮新建与历史并行优化，支持高相似历史笔记分组展示并在重复风险较高时强化新建提示。
新增 Quick Note 第九轮查看体验优化，为历史笔记结果补充完整内容预览与时间信息提示，便于不改动当前输入就先判断旧笔记内容。
新增 Quick Note 第十轮收尾优化，统一仓储与搜索排序规则，并补充特殊视图别名与边界测试。
新增 Quick Note 第11轮管理入口一致化，补充 all 视图、视图间导航与数量提示。
新增 Quick Note 第12轮多行编辑入口，支持通过独立编辑器创建长笔记和修改已有笔记。
新增 Quick Note 第13轮结构化管理基础，支持标签提取、归档以及按标签与归档视图过滤。
收束 Quick Note 主流程，抽离展示与特殊视图辅助类，降低主入口复杂度。
补充《设计文档》，系统梳理主程序架构、查询链路、插件机制、数据存储与本仓库定制插件设计。
随手记切换存储位置时新增自动迁移与目标路径合并逻辑，并补充设置提示与迁移测试覆盖。
随手记存储路径设置新增“浏览”按钮，支持通过文件选择器回填 notes.json 目标路径。
随手记 notes.json 存储改为直接写入可读中文内容，避免将中文笔记序列化为 `\uXXXX` 转义文本。
在《非技术用户使用说明》中补充随手记操作指南，说明保存、标签、归档与按标签查看的具体用法。
随手记保存与加载时自动从正文剥离 `#标签` 到 `Tags` 字段，并在编辑时自动回填标签文本以避免标签丢失。
为随手记保存与更新补充主窗口内轻提示，并在提示停留数秒后再自动关闭搜索窗口，增强即时反馈体验。
新增本机安全部署脚本，限制仅同步托管 DLL 与目标插件目录，避免覆盖安装版启动层和运行时文件导致程序无法启动。
更新《随手记功能设计方案》，从阶段 4 移除连续记录模式与可配置保存后行为，阶段 4 聚焦多行编辑与从结果进入编辑。
随手记阶段 5 第一轮：扩展 NoteRepository，新增 SetTags、无上限 GetAllNotes、Reload，并补充标签合并与仓储测试。
随手记阶段 5 第二轮：设置页改为 TabControl，拆分存储设置与笔记管理占位页，并接入刷新与笔记统计。
随手记阶段 5 第三轮：笔记管理 Tab 新增 ListView 表格，展示摘要、标签、时间、置顶/归档列，并支持每页 50 条分页。
随手记阶段 5 第四轮：笔记管理表格支持双击/按钮编辑、删除确认、置顶与归档切换。
随手记阶段 5 第五轮：笔记管理页新增关键词/状态筛选、独立标签编辑对话框，并通过 SetTags 保存标签。
随手记阶段 5 第六轮：启动器新增本周视图与打开笔记管理器入口，并补充本周笔记仓储查询测试。
随手记阶段 5 第七轮：笔记管理页支持多选批量归档/加标签/删除、列头排序、导出 TXT/MD 与打开 notes.json 文件夹。
随手记阶段 6 第一轮：笔记管理页补全导出能力，支持导出筛选结果/全部笔记（TXT/MD）与一键备份 notes.json。
随手记阶段 6 第二轮：笔记管理页支持从 TXT/MD/JSON 导入，提供拆分选项、重复跳过与 JSON 按 ID 合并。
随手记阶段 6 第三轮：笔记模型预留 Source/LastViewedAt 字段并兼容旧 JSON，补充 SQLite 存储评估结论（暂留 JSON）。
将代码工程从 Flow.Launcher 重命名为 Creta（解决方案/程序集/插件目录与显示名），插件 API 仍保留 Flow.Launcher.Plugin 以兼容 Localization 与社区插件，并支持从旧 AppData\\FlowLauncher 迁移用户数据。
验证 Creta 工程重命名：结构/插件命名空间一致、Debug 构建 0 错误、产物含 Creta.exe，单元测试 283 通过；并修正 plugin.json 仓库地址残留。
按重命名盘点收尾：修正 PluginsManager 清单链接，并为 JsonRPC 同时兼容 Creta.* 与旧版 Flow.Launcher.* 主机 API 前缀。
新增《随手记截图附件设计方案》，明确旁路 attachments 目录 + 剪贴板粘贴优先的截图落笔记路径，并与 JSON/SQLite 评估结论对齐。
